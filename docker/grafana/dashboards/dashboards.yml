apiVersion: 1

providers:
  - name: MasakCook
    orgId: 1
    folder: MasakCook
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
